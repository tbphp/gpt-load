# 新版品牌资源

来自用户确认的 GPT-Load Coral Anchor 资源包，以下 SVG 均原样复制，未修改路径、颜色或比例：

- `logo-light.svg`：原 `svg/logo-horizontal-color.svg`，浅色背景使用。
- `logo-dark.svg`：原 `svg/logo-horizontal-dark.svg`，深色背景使用。
- `icon.svg`：原 `web/icon.svg`，用于收起侧栏及新版浏览器图标。

品牌色为 Coral `#DB7458`、Ink `#282630`、Cream `#FAF7F0`。横版 Logo 保持 4:1 比例，窄位置使用专门的方形图标。界面主按钮使用更深的珊瑚色以保证白色文字对比度。

仅由 modern 引用。旧版品牌和全局 HTML 的 favicon 保持不变；新版启动后替换当前页面的 favicon。
