name: Release 2.x

on:
  push:
    tags:
      - "v2.*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate-tag:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      version: ${{ steps.tag.outputs.version }}
      prerelease: ${{ steps.tag.outputs.prerelease }}
      image_exact: ${{ steps.tag.outputs.image_exact }}
      image_minor: ${{ steps.tag.outputs.image_minor }}
      image_major: ${{ steps.tag.outputs.image_major }}
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate strict 2.x SemVer tag
        id: tag
        shell: bash
        run: |
          # release-tag-validation:start
          tag="${GITHUB_REF_NAME}"
          semver_regex='^v2\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?$'
          if [[ ! "${tag}" =~ ${semver_regex} ]]; then
            printf 'invalid 2.x release tag\n' >&2
            exit 1
          fi

          version="${tag#v}"
          core="${version%%-*}"
          prerelease=false
          if [[ "${version}" == *-* ]]; then
            prerelease=true
            prerelease_value="${version#*-}"
            IFS='.' read -r -a identifiers <<<"${prerelease_value}"
            for identifier in "${identifiers[@]}"; do
              if [[ "${identifier}" =~ ^[0-9]+$ ]] &&
                [[ "${identifier}" != "0" ]] &&
                [[ "${identifier}" == 0* ]]; then
                printf 'numeric prerelease identifiers cannot contain leading zeroes\n' >&2
                exit 1
              fi
            done
          fi

          IFS='.' read -r major minor _ <<<"${core}"
          {
            printf 'version=%s\n' "${tag}"
            printf 'prerelease=%s\n' "${prerelease}"
            printf 'image_exact=%s\n' "${tag}"
            printf 'image_minor=%s.%s\n' "${major}" "${minor}"
            printf 'image_major=%s\n' "${major}"
          } >>"${GITHUB_OUTPUT}"
          # release-tag-validation:end

      - name: Require tag commit in v2 history
        run: |
          git fetch --no-tags origin v2
          git merge-base --is-ancestor "${GITHUB_SHA}" origin/v2

  verify-and-build-web:
    needs: validate-tag
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Validate and build web UI
        uses: ./.github/actions/web-ci

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Check Go formatting
        run: test -z "$(gofmt -l .)"

      - name: Check module graph
        run: go mod tidy -diff

      - name: Run Go vet
        run: go vet ./...

      - name: Build backend
        run: go build -trimpath -o gpt-load .

      - name: Run race-enabled tests
        run: go test -race . ./internal/...

      - name: Install Chromium for browser E2E
        run: pnpm --dir web exec playwright install --with-deps chromium

      - name: Run production browser E2E
        run: pnpm --dir web run test:e2e

      - name: Check repository invariants
        run: git diff --check

      - name: Upload verified web dist
        uses: actions/upload-artifact@v4
        with:
          name: verified-web-dist
          path: internal/webui/dist
          if-no-files-found: error
          retention-days: 14

  build-binaries:
    needs: verify-and-build-web
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: true
      matrix:
        include:
          - goos: linux
            goarch: amd64
            filename: gpt-load-linux-amd64
          - goos: linux
            goarch: arm64
            filename: gpt-load-linux-arm64
          - goos: darwin
            goarch: amd64
            filename: gpt-load-macos-amd64
          - goos: darwin
            goarch: arm64
            filename: gpt-load-macos-arm64
          - goos: windows
            goarch: amd64
            filename: gpt-load-windows-amd64.exe
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Download verified web dist
        uses: actions/download-artifact@v4
        with:
          name: verified-web-dist
          path: internal/webui/dist

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Build release binary
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          mkdir -p release
          go build -trimpath \
            -ldflags "-s -w -X gpt-load/internal/platform/version.Version=${{ github.ref_name }}" \
            -o "release/${{ matrix.filename }}" .

      - name: Upload release binary
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.filename }}
          path: release/${{ matrix.filename }}
          if-no-files-found: error
          retention-days: 14

  package-checksums:
    needs: build-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Download all release binaries
        uses: actions/download-artifact@v4
        with:
          pattern: binary-gpt-load-*
          path: release
          merge-multiple: true

      - name: Generate SHA256SUMS
        working-directory: release
        run: |
          sha256sum \
            gpt-load-linux-amd64 \
            gpt-load-linux-arm64 \
            gpt-load-macos-amd64 \
            gpt-load-macos-arm64 \
            gpt-load-windows-amd64.exe >SHA256SUMS
          sha256sum --check SHA256SUMS

      - name: Upload complete release assets
        uses: actions/upload-artifact@v4
        with:
          name: release-assets
          path: release
          if-no-files-found: error
          retention-days: 14

  native-artifact-smoke:
    needs:
      - build-binaries
      - package-checksums
    timeout-minutes: 10
    strategy:
      fail-fast: true
      matrix:
        include:
          - runner: ubuntu-24.04
            filename: gpt-load-linux-amd64
          - runner: ubuntu-24.04-arm
            filename: gpt-load-linux-arm64
          - runner: macos-15-intel
            filename: gpt-load-macos-amd64
          - runner: macos-15
            filename: gpt-load-macos-arm64
          - runner: windows-latest
            filename: gpt-load-windows-amd64.exe
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Download complete release assets
        uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: native-smoke

      - name: Smoke POSIX native artifact
        if: runner.os != 'Windows'
        shell: bash
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_SMOKE_BINARY: ${{ github.workspace }}/native-smoke/${{ matrix.filename }}
          RELEASE_SMOKE_CHECKSUM_FILE: ${{ github.workspace }}/native-smoke/SHA256SUMS
        run: .github/scripts/release-native-smoke.sh

      - name: Smoke Windows native artifact
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_SMOKE_BINARY: ${{ github.workspace }}/native-smoke/${{ matrix.filename }}
          RELEASE_SMOKE_CHECKSUM_FILE: ${{ github.workspace }}/native-smoke/SHA256SUMS
        run: |
          & .github/scripts/release-native-smoke.ps1
          exit $LASTEXITCODE

  docker-smoke:
    needs:
      - validate-tag
      - verify-and-build-web
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Build and smoke local image
        shell: bash
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_SMOKE_SUFFIX: ${{ github.run_id }}
        run: .github/scripts/release-docker-smoke.sh

  capture-channel-baseline:
    needs:
      - validate-tag
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      packages: read
    outputs:
      ghcr_latest_digest: ${{ steps.snapshot.outputs.ghcr_latest_digest }}
      dockerhub_latest_digest: ${{ steps.snapshot.outputs.dockerhub_latest_digest }}
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR for baseline inspection
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub for baseline inspection
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Capture latest digests before publication
        id: snapshot
        shell: bash
        run: |
          set -euo pipefail
          ghcr_latest_digest="$(
            .github/scripts/release-image-digest.sh ghcr.io/tbphp/gpt-load:latest
          )"
          dockerhub_latest_digest="$(
            .github/scripts/release-image-digest.sh tbphp/gpt-load:latest
          )"
          printf 'ghcr_latest_digest=%s\n' \
            "${ghcr_latest_digest}" >>"${GITHUB_OUTPUT}"
          printf 'dockerhub_latest_digest=%s\n' \
            "${dockerhub_latest_digest}" >>"${GITHUB_OUTPUT}"

  publish-github:
    needs:
      - validate-tag
      - verify-and-build-web
      - build-binaries
      - package-checksums
      - native-artifact-smoke
      - docker-smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Download complete release assets
        uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: release

      - name: Verify release checksums
        working-directory: release
        run: sha256sum --check SHA256SUMS

      - name: Publish one GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: GPT-Load ${{ github.ref_name }}
          prerelease: ${{ needs.validate-tag.outputs.prerelease }}
          fail_on_unmatched_files: true
          files: |
            release/gpt-load-linux-amd64
            release/gpt-load-linux-arm64
            release/gpt-load-macos-amd64
            release/gpt-load-macos-arm64
            release/gpt-load-windows-amd64.exe
            release/SHA256SUMS
          body: |
            GPT-Load 2.x is a greenfield rewrite and is not data-compatible with 1.x.
            Do not open a 1.x database with this release; rebuild configuration and cut over manually.

            This release provides native OpenAI, Anthropic, and Gemini protocol gateways,
            an embedded management UI, scheduling and health controls, request logs,
            usage estimates, and model price management. It is single-instance and SQLite-only.

            GPT-Load uses a unified data/control dual-plane architecture: Groups define upstream
            routing and key pools, AccessKeys authorize data-plane access, and model discovery
            imports available models from compatible upstreams.

            Docker runs as a fixed non-root user with a named volume. The 2.x image channels
            do not move `latest`. Back up the database together with auth/encryption key files.
            Usage and cost are best-effort estimates, not a billing ledger or provider invoice.

            Follow the [Operations Runbook](https://app.notion.com/p/3a95e49ce6ae813db7f9c7d6b8d83f02)
            for deployment and backup/restore, and the
            [1.x cutover and rollback](https://app.notion.com/p/3a95e49ce6ae813db7f9c7d6b8d83f02)
            procedure. This release contains five raw binaries plus `SHA256SUMS`.

            Usage quality can be `missing`, `partial`, or `unpriced`; a compatible upstream may
            never provide authoritative terminal usage. Cost remains an estimate, not a provider
            bill. GPT-Load 2.0.0 does not support encryption key rotation.

            Built-in prices were reviewed on 2026-07-27 against the official
            [OpenAI](https://developers.openai.com/api/docs/pricing),
            [Anthropic](https://platform.claude.com/docs/en/about-claude/pricing), and
            [Gemini](https://ai.google.dev/gemini-api/docs/pricing) sources.

  publish-images:
    needs:
      - validate-tag
      - capture-channel-baseline
      - verify-and-build-web
      - build-binaries
      - package-checksums
      - native-artifact-smoke
      - docker-smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/tbphp/gpt-load
            tbphp/gpt-load
          tags: |
            type=raw,value=${{ needs.validate-tag.outputs.image_exact }}
            type=raw,value=${{ needs.validate-tag.outputs.image_minor }},enable=${{ needs.validate-tag.outputs.prerelease == 'false' }}
            type=raw,value=${{ needs.validate-tag.outputs.image_major }},enable=${{ needs.validate-tag.outputs.prerelease == 'false' }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ github.ref_name }}

      - name: Build and publish multi-platform images
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ github.ref_name }}

  post-publish-native-smoke:
    needs:
      - publish-github
      - publish-images
    timeout-minutes: 15
    permissions:
      contents: read
    strategy:
      fail-fast: true
      matrix:
        include:
          - runner: ubuntu-24.04
            filename: gpt-load-linux-amd64
          - runner: ubuntu-24.04-arm
            filename: gpt-load-linux-arm64
          - runner: macos-15-intel
            filename: gpt-load-macos-amd64
          - runner: macos-15
            filename: gpt-load-macos-arm64
          - runner: windows-latest
            filename: gpt-load-windows-amd64.exe
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Download published GitHub Release assets
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p public-release
          gh release download "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --dir public-release

      - name: Smoke published POSIX native artifact
        if: runner.os != 'Windows'
        shell: bash
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_SMOKE_BINARY: ${{ github.workspace }}/public-release/${{ matrix.filename }}
          RELEASE_SMOKE_CHECKSUM_FILE: ${{ github.workspace }}/public-release/SHA256SUMS
        run: .github/scripts/release-native-smoke.sh

      - name: Smoke published Windows native artifact
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_SMOKE_BINARY: ${{ github.workspace }}/public-release/${{ matrix.filename }}
          RELEASE_SMOKE_CHECKSUM_FILE: ${{ github.workspace }}/public-release/SHA256SUMS
        run: .github/scripts/release-native-smoke.ps1

  post-publish-verify:
    needs:
      - validate-tag
      - capture-channel-baseline
      - publish-github
      - publish-images
      - post-publish-native-smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout tag commit
        uses: actions/checkout@v7

      - name: Download and verify exact GitHub Release asset inventory
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p public-release
          gh release download "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --dir public-release

          cat >/tmp/expected-release-assets <<'ASSETS'
          SHA256SUMS
          gpt-load-linux-amd64
          gpt-load-linux-arm64
          gpt-load-macos-amd64
          gpt-load-macos-arm64
          gpt-load-windows-amd64.exe
          ASSETS
          find public-release -mindepth 1 -maxdepth 1 -type f -exec basename {} \; |
            sort >/tmp/actual-release-assets
          asset_count="$(wc -l </tmp/actual-release-assets | tr -d ' ')"
          test "${asset_count}" = "6"
          cmp /tmp/expected-release-assets /tmp/actual-release-assets

          cd public-release
          test "$(wc -l <SHA256SUMS | tr -d ' ')" = "5"
          awk '{print $2}' SHA256SUMS | sort >/tmp/checksummed-release-assets
          tail -n +2 /tmp/expected-release-assets >/tmp/expected-checksummed-release-assets
          cmp /tmp/expected-checksummed-release-assets /tmp/checksummed-release-assets
          sha256sum --check SHA256SUMS

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR for manifest verification
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub for manifest verification
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Verify published image manifests
        shell: bash
        run: |
          set -euo pipefail
          ghcr_exact_digest=
          dockerhub_exact_digest=
          for repository in ghcr.io/tbphp/gpt-load tbphp/gpt-load; do
            exact="${repository}:${{ needs.validate-tag.outputs.image_exact }}"
            docker buildx imagetools inspect "${exact}" >/tmp/manifest
            grep -F 'linux/amd64' /tmp/manifest
            grep -F 'linux/arm64' /tmp/manifest
            exact_digest="$(
              docker buildx imagetools inspect "${exact}" --format '{{.Manifest.Digest}}'
            )"
            test -n "${exact_digest}"
            if [[ "${repository}" == ghcr.io/* ]]; then
              ghcr_exact_digest="${exact_digest}"
              latest_image="ghcr.io/tbphp/gpt-load:latest"
              expected_latest_digest="${{ needs.capture-channel-baseline.outputs.ghcr_latest_digest }}"
            else
              dockerhub_exact_digest="${exact_digest}"
              latest_image="tbphp/gpt-load:latest"
              expected_latest_digest="${{ needs.capture-channel-baseline.outputs.dockerhub_latest_digest }}"
            fi
            latest_digest="$(.github/scripts/release-image-digest.sh "${latest_image}")"
            test "${latest_digest}" = "${expected_latest_digest}"

            if [[ "${{ needs.validate-tag.outputs.prerelease }}" == "false" ]]; then
              for alias in \
                "${{ needs.validate-tag.outputs.image_minor }}" \
                "${{ needs.validate-tag.outputs.image_major }}"; do
                alias_digest="$(
                  docker buildx imagetools inspect \
                    "${repository}:${alias}" \
                    --format '{{.Manifest.Digest}}'
                )"
                test "${alias_digest}" = "${exact_digest}"
              done
            fi
          done
          test "${ghcr_exact_digest}" = "${dockerhub_exact_digest}"

      - name: Run full runtime smoke for both published exact images
        shell: bash
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          for source_and_suffix in \
            "ghcr.io/tbphp/gpt-load:${{ needs.validate-tag.outputs.image_exact }}|ghcr" \
            "tbphp/gpt-load:${{ needs.validate-tag.outputs.image_exact }}|dockerhub"; do
            source_image="${source_and_suffix%%|*}"
            smoke_suffix="${source_and_suffix##*|}"
            RELEASE_SMOKE_SOURCE_IMAGE="${source_image}" \
              RELEASE_SMOKE_SUFFIX="${GITHUB_RUN_ID}-${smoke_suffix}" \
              .github/scripts/release-docker-smoke.sh
          done
